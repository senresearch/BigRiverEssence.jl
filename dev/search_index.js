var documenterSearchIndex = {"docs":
[{"category":"section","location":"plskern_tutorial/#PLS:-Partial-Least-Squares-Regression","page":"Partial Least Squares kernal Regression","text":"Partial Least Squares or PLS is a regression technique which is used for predicting a response Y using a set of predictors X using a small number of latent components. The latent components are linear combinition of predictors which can be regressed on instead of the original set of regressors X. This is particularly helpful if we have a large number of correlated predictors. In this scenario, oridinary least squares fails whereas PLS chooses its components to capture the directions of X that are most predictive of Y.   \n\nIn this documentation, we will depomstrate implementation of Plskern using BigRiverEssence.plskern on a gasoline dataset. ","title":"PLS: Partial Least Squares Regression"},{"category":"section","location":"plskern_tutorial/#The-method","page":"Partial Least Squares kernal Regression","text":"Let Y be response variable and X be a matrix of regressors. PLS finds a direction w (one as a time) in the predictor space such that it maximizes the covariance between the projected predictors Xw and the response Y. It considers the following optimization problem: max_w  operatornamecov(Xw Y) quad textsubject to  w_2 = 1\n\nThe scores t = Xw are the values of the samples along that component. The predictor and response loadings (p and q) are used for describing how X and Y are related to the scores. We then deflate or remove the component from the cross covariance. This enables the next component to captures new predictive structure rather than repeating what has been found. After we obtain k components, prediction reduces to a linear model hat Y = textintercept + X B, where the coefficient matrix B is assembled from the accumulated weights and loadings.\n\nplskern of BigRiverEssence implements the improved kernel algorithms used by Dayal & MacGregor (1997) where they use  cross-product matrices for efficient computation. The number of components, nlv, is the main tuning parameter of plskern where  a small value leads underfitting and vice versa.","title":"The method"},{"category":"section","location":"plskern_tutorial/#The-data","page":"Partial Least Squares kernal Regression","text":"The gasoline dataset (near-infrared spectra and octane numbers for 60 gasoline samples) is from Kalivas (1997) [1], obtained via the R pls package. The NIR spectra were measured as log(1/R) from 900 to 1700 nm in 2 nm steps, giving 401 wavelengths.\n\nusing BigRiverEssence, DelimitedFiles, Plots, Statistics, Random\n\n\n\n\ndatadir = joinpath(pkgdir(BigRiverEssence), \"reference_Data\", \"gasolinedata\")\nNIR    = readdlm(joinpath(datadir, \"NIR.csv\"),    ',', Float64)   # 60 × 401 spectra\noctane = vec(readdlm(joinpath(datadir, \"octane.csv\"), ',', Float64))  # 60 responses\nwl     = collect(900:2:1700)         # the known wavelength grid (nm)\nsize(NIR), length(octane)","title":"The data"},{"category":"section","location":"plskern_tutorial/#Train/test-split","page":"Partial Least Squares kernal Regression","text":"Now, since PLS is a predictive model, we consider a hold out test set. We use a random seed for reproduction.\n\nRandom.seed!(42)\nidx = shuffle(1:size(NIR, 1))\ntrain, test = idx[1:40], idx[41:end]\n\nXtr = Matrix{Float64}(NIR[train, :]); ytr = reshape(Float64.(octane[train]), :, 1)\nXte = Matrix{Float64}(NIR[test, :]);  yte = reshape(Float64.(octane[test]),  :, 1)\n\n\nIt is important to keep in mind that plskern takes the response as a matrix which gives it flexibility to handles more than one responses uniformly. Hence we reshaped ytr and yte to ntimes 1 matrices. ","title":"Train/test split"},{"category":"section","location":"plskern_tutorial/#Fitting-the-model-plskern","page":"Partial Least Squares kernal Regression","text":"We now fit a PLS model with 5 latent components.\n\nm = plskern(Xtr, ytr; nlv = 5)\n\nThe function returns the full PLS factorization: the weights W and R, the predictor and response loadings P and Q, the scores T, and the centering means and scales.\n\n@show size(m.W)\n@show size(m.P)\n@show size(m.Q)\n@show size(m.T)","title":"Fitting the model plskern"},{"category":"section","location":"plskern_tutorial/#Predicting-with-plskernpredict","page":"Partial Least Squares kernal Regression","text":"We now predict octane for the held-out samples in the test set and compare with the measured values.\n\nŷ = plskernpredict(m, Xte)\n\nrmse = sqrt(mean((vec(ŷ) .- vec(yte)).^2))     # test-set prediction error\n@show rmse\nscatter(vec(yte), vec(ŷ); legend = false,\n    xlabel = \"measured octane\", ylabel = \"predicted octane\",\n    title = \"PLS prediction on held-out samples\")\nlims = extrema(vcat(vec(yte), vec(ŷ)))\nplot!(collect(lims), collect(lims); color = :black, linestyle = :dash)\n\nWe see from the above plot that the predicted octane values fall veryclose to the dashed 45 line of perfect prediction. We also get a test-set RMSE of about 0.1822 octane units. This shows a good calibration from spectra alone, on samples the model never saw during fitting.","title":"Predicting with plskernpredict"},{"category":"section","location":"plskern_tutorial/#The-regression-coefficients-with-plskerncoef","page":"Partial Least Squares kernal Regression","text":"The function plskerncoef is used for assembling the coefficient vector B and intercept that map a raw spectrum directly to a predicted octane. We can  plot B against wavelength to see which spectral regions drive the prediction.\n\nB, intercept = plskerncoef(m)\n\nplot(wl, vec(B); legend = false,\n    xlabel = \"wavelength (nm)\", ylabel = \"regression coefficient\",\n    title = \"PLS coefficients across the NIR spectrum\")\nhline!([0], color = :black, alpha = 0.3)\n\nwe see from the plot that most wavelengths carry small coefficients, with few high peaks where the spectral regions are most informative about octane. This is the interpretable payoff of a linear calibration where the coefficients point back to the chemistry. ","title":"The regression coefficients with plskerncoef"},{"category":"section","location":"plskern_tutorial/#Projecting-with-plskerntransform","page":"Partial Least Squares kernal Regression","text":"The other function plskerntransform is used for projecting samples onto the PLS latent space or their scores. If we color the scores by octane, it will shows that the components are organized around the response. This is the supervised counterpart to the variance-only components of PCA.\n\nTtr = plskerntransform(m, Xtr)\n\nscatter(Ttr[:, 1], Ttr[:, 2]; zcolor = vec(ytr), colorbar = true, legend = false,\n    xlabel = \"PLS score 1\", ylabel = \"PLS score 2\",\n    title = \"Samples in PLS latent space (colored by octane)\")\n\nAfter coloring the score scatter by octane, in the above plot, we get to know what makes PLS supervised. We see the samples lining up along the first component by their octane value where dark (low-octane) points to the left and bright (high-octane) to the right. The first PLS component is giving the direction of the spectra most predictive of octane, not merely the direction of greatest spectral variance as in PCA. That is why regressing on a few PLS scores captures the response so efficiently.","title":"Projecting with plskerntransform"},{"category":"section","location":"plskern_tutorial/#Summary","page":"Partial Least Squares kernal Regression","text":"We saw that from 60 spectra with 401 correlated predictors, plskern were able to built a handful of latent components that predict octane accurately on held-out samples. This is the setting where PLS outperforms ordinary regression. \n\nplskern is most suitable in regression problems where there are huge number of correlated variables where ordinary least squares. In this example, we used the gasoline dataset which has 401 variables but only 60 samples. plskern can be used in any similar type of datasets.","title":"Summary"},{"category":"section","location":"plskern_tutorial/#References","page":"Partial Least Squares kernal Regression","text":"[1] Kalivas, J. H. (1997). Two data sets of near infrared spectra. Chemometrics and     Intelligent Laboratory Systems, 37, 255–259.","title":"References"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Public-functions-and-types","page":"API Reference","text":"These are the functions and types exported by BigRiverEssence.jl.","title":"Public functions and types"},{"category":"section","location":"api/#Internal-functions","page":"API Reference","text":"Internal helpers, documented for reference. These are not part of the public API and may change.","title":"Internal functions"},{"category":"method","location":"api/#BigRiverEssence.cca-Tuple{Matrix{Float64}, Matrix{Float64}}","page":"API Reference","text":"cca(X::Matrix{Float64}, Y::Matrix{Float64}; method::Symbol = :svd,\n\toutdim::Int = min(size(X,1), size(Y,1)))\n\nFit a canonical correlation analysis (CCA) between two sets of variables, after Weenink (2003)\n\nArguments\n\nX::Matrix{Float64}: 2d array of floats; the first variable set, with variables in ROWS and observations in COLUMNS (dx×n)\nY::Matrix{Float64}: 2d array of floats; the second variable set (dy×n), with the same number of observations (columns) as X\nmethod::Symbol: The solver, :svd (works directly from the centered data, more stable) or :cov (works from covariance matrices). Both give the same result. Defaults to :svd\noutdim::Int: The number of canonical components to return; must be in 1:min(dx, dy). Defaults to min(dx, dy)\n\nValue\n\nA ccaStructure holding the X and Y means, the canonical directions for each side, and the canonical correlations (descending). CCA finds pairs of directions, one in X-space and one in Y-space, whose projected variates are maximally correlated; the k-th pair is the most correlated subject to being uncorrelated with the previous k−1. The default solver follows the SVD-based approach of Weenink (2003), which avoids forming covariance matrices for numerical stability. Warns when n is not comfortably larger than dx and dy, where CCA becomes unstable\n\n\n\n\n\n","title":"BigRiverEssence.cca"},{"category":"method","location":"api/#BigRiverEssence.cca_transform-Tuple{ccaStructure, AbstractMatrix, Symbol}","page":"API Reference","text":"cca_transform(M::ccaStructure, Z::AbstractMatrix, c::Symbol)\n\nProject data onto the canonical directions of a fitted CCA model\n\nArguments\n\nM::ccaStructure: A fitted CCA model, as returned by cca\nZ::AbstractMatrix: 2d array of floats; the data to project, with variables in rows and observations in columns, matching the side selected by c\nc::Symbol: Which side to project, :x (using the X directions and X mean) or :y (using the Y directions and Y mean)\n\nValue\n\n2d array of floats; the p×n matrix of canonical variates (each observation's coordinates along the canonical directions). Throws an ArgumentError if c is neither :x nor :y\n\n\n\n\n\n","title":"BigRiverEssence.cca_transform"},{"category":"method","location":"api/#BigRiverEssence.jive-Tuple{Vector{Matrix{Float64}}, Int64, Vector{Int64}}","page":"API Reference","text":"jive(Xs::Vector{Matrix{Float64}}, r::Int, ri::Vector{Int}; kwargs...)\n\nPositional-argument form of jive for supplying the joint rank r and individual ranks ri directly\n\nArguments\n\nXs::Vector{Matrix{Float64}}: The data blocks, each pᵢ×n\nr::Int: The joint rank\nri::Vector{Int}: The individual ranks, one per block\nkwargs...: Any keyword arguments accepted by the keyword form of jive\n\nValue\n\nA jiveStructure; identical to calling jive(Xs; r = r, ri = ri, kwargs...)\n\n\n\n\n\n","title":"BigRiverEssence.jive"},{"category":"method","location":"api/#BigRiverEssence.jive-Tuple{Vector{Matrix{Float64}}}","page":"API Reference","text":"jive(Xs::Vector{Matrix{Float64}}; r = nothing, ri = nothing, scale = true,\n\t center = true, tol = nothing, maxiter = 1000, nperm = 100, alpha = 0.05)\n\nFit a JIVE (Joint and Individual Variation Explained) decomposition of several data blocks sharing a common set of observations\n\nArguments\n\nXs::Vector{Matrix{Float64}}: The data blocks, each pᵢ×n with the SAME number of columns n (observations); rows (features) may differ per block\nr: The joint rank; if nothing, estimated by permutation test. Defaults to nothing\nri: The vector of individual ranks; if nothing, estimated by permutation test. Defaults to nothing\nscale::Bool: Whether to scale each block by its Frobenius norm (and √(total elements)) so blocks contribute comparably. Defaults to true\ncenter::Bool: Whether to row-center each block. Defaults to true\ntol: The convergence threshold; if nothing, set to 1e-6·‖stacked data‖. Defaults to nothing\nmaxiter::Int: The maximum core-fit iterations. Defaults to 1000\nnperm::Int: The permutations per rank test (when ranks are estimated). Defaults to 100\nalpha::Float64: The significance level for the rank test. Defaults to 0.05\n\nValue\n\nA jiveStructure with the joint and individual decompositions. Each block is first row-centered and Frobenius-scaled so no block dominates by sheer magnitude. If the joint and individual ranks are not supplied, they are estimated by a permutation test; the decomposition is then computed by alternating between estimating the shared joint structure and each block's individual structure until they converge. The result splits each block into joint structure (a common row space across all blocks) and block-specific individual structure orthogonal to it, after Lock et al. (2013)\n\n\n\n\n\n","title":"BigRiverEssence.jive"},{"category":"method","location":"api/#BigRiverEssence.pca-Tuple{Matrix{Float64}}","page":"API Reference","text":"pca(X::Matrix{Float64}; k::Int = minimum(size(X)),\n\tstandardize::Bool = false, method::Symbol = :auto)\n\nFit a principal component analysis (PCA) model\n\nArguments\n\nX::Matrix{Float64}: 2d array of floats; observations (rows) by features (columns)\nk::Int: The number of components to retain; 1 ≤ k ≤ min(n,p). Defaults to min(n,p)\nstandardize::Bool: Whether to scale columns to unit variance in addition to centering. Defaults to false (center only)\nmethod::Symbol: :auto (pick by shape — :cov when n ≥ p, :svd when p > n), :cov (eigendecomposition of the p×p covariance), or :svd (SVD of the centered data). Defaults to :auto\n\nValue\n\nA pcaStructure holding the column means, scales, k loadings, component variances, and proportion of variance explained\n\n\n\n\n\n","title":"BigRiverEssence.pca"},{"category":"method","location":"api/#BigRiverEssence.pca_invtransform-Tuple{pcaStructure, Matrix{Float64}}","page":"API Reference","text":"pca_invtransform(m::pcaStructure, scores::Matrix{Float64})\n\nReconstruct data in the original feature space from principal-component scores\n\nArguments\n\nm::pcaStructure: A fitted PCA model, as returned by pca\nscores::Matrix{Float64}: 2d array of floats; the n×k matrix of principal-component scores to invert, with k matching the number of components retained in m\n\nValue\n\n2d array of floats; the n×p reconstruction in the original units. Exact only when all components are retained (k = p); otherwise a low-rank approximation\n\n\n\n\n\n","title":"BigRiverEssence.pca_invtransform"},{"category":"method","location":"api/#BigRiverEssence.pca_transform-Tuple{pcaStructure, Matrix{Float64}}","page":"API Reference","text":"pca_transform(m::pcaStructure, X::Matrix{Float64})\n\nProject data onto the principal directions of a fitted PCA model\n\nArguments\n\nm::pcaStructure: A fitted PCA model, as returned by pca\nX::Matrix{Float64}: 2d array of floats; the observations (rows) by features (columns) to project, with the same features as the training data\n\nValue\n\n2d array of floats; the n×k matrix of principal-component scores\n\n\n\n\n\n","title":"BigRiverEssence.pca_transform"},{"category":"method","location":"api/#BigRiverEssence.plskern-Tuple{Matrix{Float64}, Matrix{Float64}}","page":"API Reference","text":"plskern(X::Matrix{Float64}, Y::Matrix{Float64}; nlv::Int = 2,\n\t\tstandardize::Bool = false, method::Symbol = :algo1)\n\nFit a kernel partial least squares (PLS) regression using the improved algorithms of Dayal & MacGregor (1997). The caller's X and Y are left untouched; centering and scaling are done on internal copies\n\nArguments\n\nX::Matrix{Float64}: 2d array of floats; the n×p predictor matrix\nY::Matrix{Float64}: 2d array of floats; the n×q response matrix\nnlv::Int: The number of latent variables; clamped to min(nlv, n, p). Defaults to 2\nstandardize::Bool: Whether to scale each column to unit standard deviation in addition to centering. Defaults to false (center only)\nmethod::Symbol: :algo1 (kernel via XᵀY) or :algo2 (kernel via XᵀX, faster when p ≪ n). Both give identical results. Defaults to :algo1\n\nValue\n\nA plskernStructure holding the weight (W, R), loading (P, Q), and score (T) matrices together with the centering means and scales\n\n\n\n\n\n","title":"BigRiverEssence.plskern"},{"category":"method","location":"api/#BigRiverEssence.plskerncoef-Tuple{plskernStructure}","page":"API Reference","text":"plskerncoef(m::plskernStructure; nlv::Int = size(m.R, 2))\n\nAssemble the regression coefficient matrix and intercept from a fitted PLS model\n\nArguments\n\nm::plskernStructure: A fitted PLS model, as returned by plskern\nnlv::Int: The number of latent variables to include; clamped to the number fitted. Defaults to all fitted components\n\nValue\n\nA tuple (B, intercept) where B is the p×q coefficient matrix and intercept is the 1×q intercept, such that a prediction is intercept .+ Xnew * B. The scales are folded in so B maps raw (uncentered, unscaled) X to Y\n\n\n\n\n\n","title":"BigRiverEssence.plskerncoef"},{"category":"method","location":"api/#BigRiverEssence.plskernpredict-Tuple{plskernStructure, Any}","page":"API Reference","text":"plskernpredict(m::plskernStructure, Xnew::Matrix{Float64}; nlv::Int = size(m.R, 2))\n\nPredict responses for new observations from a fitted PLS model\n\nArguments\n\nm::plskernStructure: A fitted PLS model, as returned by plskern\nXnew::Matrix{Float64}: 2d array of floats; the new observations (rows) by predictors (columns), with the same p predictors as the training data\nnlv::Int: The number of latent variables to use; clamped to the number fitted. Defaults to all fitted components\n\nValue\n\n2d array of floats; the predicted n×q responses, intercept .+ Xnew * B\n\n\n\n\n\n","title":"BigRiverEssence.plskernpredict"},{"category":"method","location":"api/#BigRiverEssence.plskerntransform-Tuple{plskernStructure, Any}","page":"API Reference","text":"plskerntransform(m::plskernStructure, Xnew::Matrix{Float64}; nlv::Int = size(m.R, 2))\n\nProject new observations onto the PLS latent space (compute their X-scores)\n\nArguments\n\nm::plskernStructure: A fitted PLS model, as returned by plskern\nXnew::Matrix{Float64}: 2d array of floats; the new observations (rows) by predictors (columns), with the same p predictors as the training data\nnlv::Int: The number of latent variables to project onto; clamped to the number fitted. Defaults to all fitted components\n\nValue\n\n2d array of floats; the n×nlv matrix of X-scores. For the training data this reproduces the stored scores m.T, since scores are linear: T = Xc·R\n\n\n\n\n\n","title":"BigRiverEssence.plskerntransform"},{"category":"method","location":"api/#BigRiverEssence.pmd-Tuple{Matrix{Float64}}","page":"API Reference","text":"pmd(X::Matrix{Float64}; sumabs::Real = 0.4,\n\tsumabsu::Union{Nothing,Real} = nothing,\n\tsumabsv::Union{Nothing,Real} = nothing,\n\tK::Int = 1, niter::Int = 20, center::Bool = true)\n\nFit a penalized matrix decomposition with L1 penalties on both factors, PMD(L1,L1)\n\nArguments\n\nX::Matrix{Float64}: 2d array of floats; the n×p data matrix\nsumabs::Real: The shared sparsity budget in (0,1], scaled internally to give the per-column L1 budgets su = √n·sumabs on u and sv = √p·sumabs on v. Smaller values give sparser factors. Ignored when both sumabsu and sumabsv are supplied. Defaults to 0.4\nsumabsu::Union{Nothing,Real}: An explicit L1 budget on each u column, in [1, √n]; overrides sumabs when given together with sumabsv. Defaults to nothing\nsumabsv::Union{Nothing,Real}: An explicit L1 budget on each v column, in [1, √p]; overrides sumabs when given together with sumabsu. Defaults to nothing\nK::Int: The number of components to extract; must be in 1:min(n,p). Defaults to 1\nniter::Int: The maximum number of power-iteration steps per component. Defaults to 20\ncenter::Bool: Whether to subtract the grand mean of X before decomposing. Defaults to true\n\nValue\n\nA pmdStructure holding the n×K left factors u, the p×K right factors v, the K weights d, the resolved budgets su and sv, the number of components K, and the grand mean removed (or NaN when center=false)\n\n\n\n\n\n","title":"BigRiverEssence.pmd"},{"category":"method","location":"api/#BigRiverEssence.scca-Tuple{Matrix{Float64}, Matrix{Float64}}","page":"API Reference","text":"scca(X::Matrix{Float64}, Y::Matrix{Float64}; penaltyx::Real = 0.3,\n\t penaltyz::Real = 0.3, K::Int = 1, niter::Int = 15, standardize::Bool = true)\n\nFit a sparse canonical correlation analysis (sparse CCA) between two sets of variables, after Witten, Tibshirani & Hastie (2009)\n\nArguments\n\nX::Matrix{Float64}: 2d array of floats; the first variable set, with variables in ROWS and observations in COLUMNS (dx×n)\nY::Matrix{Float64}: 2d array of floats; the second variable set (dy×n), same number of observations (columns) as X\npenaltyx::Real: The L1 penalty on the X canonical vectors, in (0,1]; smaller values give sparser vectors. Defaults to 0.3\npenaltyz::Real: The L1 penalty on the Y canonical vectors, in (0,1]. Defaults to 0.3\nK::Int: The number of canonical components to extract; must be in 1:min(dx,dy). Defaults to 1\nniter::Int: The maximum power-iteration steps per component. Defaults to 15\nstandardize::Bool: Whether to center and scale each variable to unit variance before fitting. Defaults to true\n\nValue\n\nAn sccaStructure holding the sparse X and Y canonical vectors (u, v), the component weights d, the sample correlations of the paired variates, the penalties, and K. Sparse CCA finds pairs of sparse vectors, one in X-space and one in Y-space, whose projected variates are maximally correlated under an L1 penalty (so each vector selects only a few variables). It uses the diagonal-covariance penalized formulation of Witten, Tibshirani & Hastie (2009): each component is a rank-1 penalized approximation of XᵀY solved by alternating soft-thresholded power iteration, after which the data are deflated before the next component\n\n\n\n\n\n","title":"BigRiverEssence.scca"},{"category":"method","location":"api/#BigRiverEssence.spc-Tuple{Any}","page":"API Reference","text":"spc(X::Matrix{Float64}; k::Int = 2, c::Real = sqrt(size(X,2))/2,\n\tstandardize::Bool = false, tol::Real = 1e-7, niter::Int = 20)\n\nFit a sparse principal component analysis using the SPC criterion (deflation variant)\n\nArguments\n\nX::Matrix{Float64}: 2d array of floats; the n×p data matrix\nk::Int: The number of sparse components to extract. Defaults to 2\nc::Real: The L1 budget on each loading, in [1, √p]; smaller values give sparser loadings, c = √p gives ordinary (dense) PCA. Defaults to √p / 2\nstandardize::Bool: Whether to scale each column to unit standard deviation in addition to centering. Defaults to false (center only)\ntol::Real: The ℓ₁ convergence tolerance per component. Defaults to 1e-7\nniter::Int: The maximum number of power-iteration steps per component. Defaults to 20\n\nValue\n\nAn spcStructure holding the column means, scales, k sparse loadings, component variances, and cumulative proportion of variance explained. Components are found sequentially: each loading is fit on the residual, then removed by rank-1 deflation before the next is extracted\n\n\n\n\n\n","title":"BigRiverEssence.spc"},{"category":"method","location":"api/#BigRiverEssence.spc_orth-Tuple{Any}","page":"API Reference","text":"spc_orth(X::Matrix{Float64}; k::Int = 2, c::Real = sqrt(size(X,2))/2,\n\t\t standardize::Bool = false, tol::Real = 1e-6, niter::Int = 20)\n\nFit a sparse principal component analysis with orthogonal scores (orthogonal variant of the SPC criterion)\n\nArguments\n\nX::Matrix{Float64}: 2d array of floats; the n×p data matrix\nk::Int: The number of sparse components to extract. Defaults to 2\nc::Real: The L1 budget on each loading, in [1, √p]; smaller values give sparser loadings. Defaults to √p / 2\nstandardize::Bool: Whether to scale each column to unit standard deviation in addition to centering. Defaults to false (center only)\ntol::Real: The ℓ₁ convergence tolerance per component. Defaults to 1e-6\nniter::Int: The maximum number of power-iteration steps per component. Defaults to 20\n\nValue\n\nAn spcStructure holding the column means, scales, k sparse loadings, component variances, and cumulative proportion of variance explained. Unlike spc, each component's scores are constrained to be orthogonal to all previous components' scores (via projection rather than deflation), so the score directions form an orthonormal set\n\n\n\n\n\n","title":"BigRiverEssence.spc_orth"},{"category":"method","location":"api/#BigRiverEssence.splsda-Tuple{Matrix{Float64}, Vector, Int64, Vector{Int64}}","page":"API Reference","text":"splsda(X::Matrix{Float64}, y::Vector, ncomp::Int, keepX::Vector{Int};\n\t   scale = true, tol = 1e-6, max_iter = 100, levels = nothing)\n\nFit a sparse PLS discriminant analysis (sPLS-DA) for multiclass problems, after Lê Cao, Boitard & Besse (2011)\n\nArguments\n\nX::Matrix{Float64}: 2d array of floats; the n×p predictor matrix\ny::Vector: The class labels, one per observation (n total)\nncomp::Int: The number of components to extract\nkeepX::Vector{Int}: The number of variables to keep per component; must have length ncomp (one sparsity budget per component)\nscale::Bool: Whether to scale columns to unit variance in addition to centering. Defaults to true\ntol::Real: The convergence tolerance on the loading iterates. Defaults to 1e-6\nmax_iter::Int: The maximum number of inner iterations per component. Defaults to 100\nlevels: An optional class ordering passed to the dummy encoding; fixes the column order of the classes. Defaults to nothing (sorted unique labels)\n\nValue\n\nAn splsdaStructure holding the X and Y variates (scores), the sparse X and dense Y loadings, the number of components, the per-component keepX, the one-hot class encoding, and the class labels. The class labels are dummy-encoded into an indicator matrix and the problem solved as a sparse PLS regression of X onto that matrix: each component is the penalized rank-1 approximation of the cross-product Mₕ = XₕᵀYₕ, solved by alternating soft-thresholded power iteration, after which X (only) is deflated by regression on its variate (Y is not deflated — the discriminant regression mode)\n\n\n\n\n\n","title":"BigRiverEssence.splsda"},{"category":"type","location":"api/#BigRiverEssence.ccaStructure","page":"API Reference","text":"ccaStructure{T}\n\nContainer for a fitted canonical correlation analysis, as returned by cca\n\nFields\n\nxmean::Vector{T}: The mean of each X variable (length dx), removed during centering\nymean::Vector{T}: The mean of each Y variable (length dy), removed during centering\nxproj::Matrix{T}: The dx×p X canonical directions (columns), one per component\nyproj::Matrix{T}: The dy×p Y canonical directions (columns), one per component\ncorrs::Vector{T}: The p canonical correlations, in [0,1] and descending\nnobs::Int: The number of observations used (or −1 when fit via the covariance path, which doesn't carry n through)\n\n\n\n\n\n","title":"BigRiverEssence.ccaStructure"},{"category":"type","location":"api/#BigRiverEssence.jiveStructure","page":"API Reference","text":"jiveStructure{T}\n\nContainer for a fitted JIVE (Joint and Individual Variation Explained) decomposition, as returned by jive\n\nFields\n\nJ::Vector{Matrix{T}}: The joint structure, one pᵢ×n matrix per data block; all blocks share the same n-dimensional joint row space\nA::Vector{Matrix{T}}: The individual structure, one pᵢ×n matrix per block, orthogonal to the joint structure\nS::Matrix{T}: The r×n joint scores (orthonormal rows), the basis of the shared joint row space\nU::Vector{Matrix{T}}: The joint loadings, one pᵢ×r block, so that J[i] = U[i]·S\nSi::Vector{Matrix{T}}: The individual scores, one rᵢ×n matrix per block\nWi::Vector{Matrix{T}}: The individual loadings, one pᵢ×rᵢ matrix per block, so that A[i] = Wi[i]·Si[i]\nr::Int: The joint rank (dimension of the shared structure)\nri::Vector{Int}: The individual ranks, one per block\n\n\n\n\n\n","title":"BigRiverEssence.jiveStructure"},{"category":"type","location":"api/#BigRiverEssence.pcaStructure","page":"API Reference","text":"pcaStructure{T}\n\nContainer for a fitted PCA model, as returned by pca\n\nFields\n\nmean::Vector{T}: The column means of the training data (length p), removed during centering\nscale::Vector{T}: The column scales (length p) — the column standard deviations when standardize=true, otherwise ones\nloadings::Matrix{T}: The p×k principal directions (right singular vectors of the centered data / leading eigenvectors of the covariance matrix), unit-norm columns ordered by decreasing explained variance\nvariances::Vector{T}: The variance explained by each of the k components (the eigenvalues, σᵢ² / (n-1))\npropOFvar::Vector{T}: The proportion of total variance explained by each component (variances ./ total variance)\n\n\n\n\n\n","title":"BigRiverEssence.pcaStructure"},{"category":"type","location":"api/#BigRiverEssence.plskernStructure","page":"API Reference","text":"plskernStructure{T}\n\nContainer for a fitted kernel PLS regression model, as returned by plskern. Holds the standard PLS factor matrices and the centering/scaling statistics\n\nFields\n\nW::Matrix{T}: The p×nlv weight matrix (unit-norm columns)\nP::Matrix{T}: The p×nlv X-loadings\nQ::Matrix{T}: The q×nlv Y-loadings\nR::Matrix{T}: The p×nlv weight matrix in the deflated basis, so that scores satisfy T = Xc·R (the SIMPLS-style weights)\nT::Matrix{T}: The n×nlv X-scores (orthogonal columns)\nxmeans::Vector{T}: The column means of X removed during centering\nxscales::Vector{T}: The column scales of X (standard deviations if standardize=true, otherwise ones)\nymeans::Vector{T}: The column means of Y removed during centering\nyscales::Vector{T}: The column scales of Y\n\n\n\n\n\n","title":"BigRiverEssence.plskernStructure"},{"category":"type","location":"api/#BigRiverEssence.pmdStructure","page":"API Reference","text":"pmdStructure{T}\n\nContainer for a fitted penalized matrix decomposition, as returned by pmd\n\nFields\n\nu::Matrix{T}: The n×K left factors (one unit-ℓ₂, L1-penalized column per component)\nv::Matrix{T}: The p×K right factors (one unit-ℓ₂, L1-penalized column per component)\nd::Vector{T}: The K nonnegative singular-value-like weights, dₖ = uₖᵀ X vₖ\nsumabsu::Float64: The resolved L1 budget on each u column (in [1, √n])\nsumabsv::Float64: The resolved L1 budget on each v column (in [1, √p])\nK::Int: The number of components\nmeanx::Float64: The grand mean removed during centering, or NaN if center=false\n\n\n\n\n\n","title":"BigRiverEssence.pmdStructure"},{"category":"type","location":"api/#BigRiverEssence.sccaStructure","page":"API Reference","text":"sccaStructure{T}\n\nContainer for a fitted sparse canonical correlation analysis, as returned by scca\n\nFields\n\nu::Matrix{T}: The dx×K sparse X canonical vectors (one unit-ℓ₂, L1-penalized column per component); nonzero entries are the selected X features\nv::Matrix{T}: The dy×K sparse Y canonical vectors; nonzero entries are the selected Y features\nd::Vector{T}: The K weights dₖ = uₖᵀ Xᵀ Z vₖ (the penalized-covariance objective values)\ncors::Vector{T}: The K sample correlations between the paired canonical variates\npenaltyx::Float64: The L1 penalty applied to the X vectors, in (0,1]\npenaltyz::Float64: The L1 penalty applied to the Y vectors, in (0,1]\nK::Int: The number of components\n\n\n\n\n\n","title":"BigRiverEssence.sccaStructure"},{"category":"type","location":"api/#BigRiverEssence.spcStructure","page":"API Reference","text":"spcStructure{T}\n\nContainer for a fitted sparse principal component analysis, as returned by spc or spc_orth\n\nFields\n\nmean::Vector{T}: The column means of the training data (length p), removed during centering\nscale::Vector{T}: The column scales (length p) — the column standard deviations when standardize=true, otherwise ones\nloadings::Matrix{T}: The p×k sparse loadings (one unit-ℓ₂, L1-penalized column per component), signs fixed by sign_consistency_opt!\nvariances::Vector{T}: The variance associated with each of the k components (dₖ² / (n-1), where dₖ is the component weight)\npropOFvar::Vector{T}: The cumulative proportion of variance explained by the first k components (see prop_var_explained)\n\n\n\n\n\n","title":"BigRiverEssence.spcStructure"},{"category":"type","location":"api/#BigRiverEssence.splsdaStructure","page":"API Reference","text":"splsdaStructure{T}\n\nContainer for a fitted sparse PLS discriminant analysis, as returned by splsda\n\nFields\n\nvariates_X::Matrix{T}: The n×ncomp X-scores (sample coordinates for plotting/classification)\nvariates_Y::Matrix{T}: The n×ncomp Y-scores (the class-side variates)\nloadings_X::Matrix{T}: The p×ncomp sparse X-loadings; the nonzero entries per column are the variables selected for that component\nloadings_Y::Matrix{T}: The K×ncomp Y-loadings (K = number of classes)\nncomp::Int: The number of components fitted\nkeepX::Vector{Int}: The number of variables kept per component (the sparsity budget)\nY_dummy::Matrix{T}: The n×K one-hot encoding of the class labels\nclasses::Vector: The class labels, in the column order of Y_dummy\n\n\n\n\n\n","title":"BigRiverEssence.splsdaStructure"},{"category":"method","location":"api/#BigRiverEssence._binary_search_opt-Tuple{Any, Any}","page":"API Reference","text":"_binary_search_opt(argu, sumabs)\n\nFind the soft-threshold that makes a soft-thresholded vector meet a target L1 budget\n\nArguments\n\nargu: 1d array of floats; the vector to be thresholded\nsumabs: Float; the target ℓ₁/ℓ₂ ratio (the L1 budget)\n\nValue\n\nFloat; the threshold d ≥ 0 such that ‖S(argu,d)‖₁/‖S(argu,d)‖₂ ≈ sumabs. Returns 0 when the vector already satisfies the budget. Implements the bisection step   bracketing d in [0, max|argu|] and halving for up to 150 iterations or until the bracket is below 1e-6\n\n\n\n\n\n","title":"BigRiverEssence._binary_search_opt"},{"category":"method","location":"api/#BigRiverEssence._cca_cov_opt-Tuple{Any, Any, Any, Any, Any, Int64}","page":"API Reference","text":"_cca_cov_opt(Cxx, Cyy, Cxy, xmean, ymean, p::Int)\n\nSolve CCA from the covariance matrices via a generalized eigenproblem\n\nArguments\n\nCxx: 2d array of floats; the dx×dx X covariance\nCyy: 2d array of floats; the dy×dy Y covariance\nCxy: 2d array of floats; the dx×dy cross-covariance\nxmean: 1d array of floats; the X means, stored in the result\nymean: 1d array of floats; the Y means, stored in the result\np::Int: The number of canonical components to return\n\nValue\n\nA ccaStructure with the top-p canonical directions and correlations (its nobs is −1, since this path works from covariances and doesn't carry n). Solves the classical canonical-correlation generalized eigenproblem (Weenink 2003), reducing through whichever side is smaller (dx ≤ dy or dx > dy) for efficiency: the squared canonical correlations are the eigenvalues, one side's directions are the eigenvectors, and the other side's are recovered and normalized to unit variance by _qnormalize!\n\n\n\n\n\n","title":"BigRiverEssence._cca_cov_opt"},{"category":"method","location":"api/#BigRiverEssence._cca_svd_opt-Tuple{Any, Any, Any, Any, Int64}","page":"API Reference","text":"_cca_svd_opt(Zx, Zy, xmean, ymean, p::Int)\n\nSolve CCA directly from the centered data via SVD (no covariance matrices formed)\n\nArguments\n\nZx: 2d array of floats; the centered X data (dx×n), OVERWRITTEN by svd!\nZy: 2d array of floats; the centered Y data (dy×n), OVERWRITTEN by svd!\nxmean: 1d array of floats; the X means, stored in the result\nymean: 1d array of floats; the Y means, stored in the result\np::Int: The number of canonical components to return\n\nValue\n\nA ccaStructure with the top-p canonical directions and correlations. Works from the SVDs of Zx and Zy directly rather than forming covariance matrices, which is the numerically stable route advocated by Weenink (2003): the canonical correlations are the singular values of the overlap between the two right-singular-vector bases, and the canonical directions are recovered by rescaling the left singular vectors so the variates have unit variance\n\n\n\n\n\n","title":"BigRiverEssence._cca_svd_opt"},{"category":"method","location":"api/#BigRiverEssence._center_scale-Tuple{AbstractMatrix}","page":"API Reference","text":"_center_scale(M::AbstractMatrix; scale = true)\n\nCenter, and optionally scale, the columns of a matrix\n\nArguments\n\nM::AbstractMatrix: 2d array of floats; the matrix to center/scale\nscale::Bool: Whether to divide each column by its (n−1) standard deviation after centering. Defaults to true\n\nValue\n\n2d array of floats; the column-centered (and, if scale=true, unit-variance) matrix. Constant columns (zero standard deviation) are guarded: their scale is treated as 1 and the column is set to all zeros rather than producing NaN. The n−1 (corrected) standard deviation matches mixOmics' colSds\n\n\n\n\n\n","title":"BigRiverEssence._center_scale"},{"category":"method","location":"api/#BigRiverEssence._fast_init_v-Tuple{Any, Any, Any}","page":"API Reference","text":"_fast_init_v(x, z, K)\n\nInitialize the Y canonical vectors for wide data via an SVD construction\n\nArguments\n\nx: 2d array of floats; the (observations × features) X block\nz: 2d array of floats; the (observations × features) Z block\nK: Int; the number of components to initialize\n\nValue\n\n2d array of floats; the K leading left singular vectors of zᵀ·(xxᵀ)^½, used as the starting v for the sparse iteration when both blocks are wider than they are tall. Building the init from xxᵀ (observations × observations) keeps the SVD small in the high-dimensional case\n\n\n\n\n\n","title":"BigRiverEssence._fast_init_v"},{"category":"method","location":"api/#BigRiverEssence._jive_perm_ranks_opt-Tuple{Vector{Matrix{Float64}}, Int64}","page":"API Reference","text":"_jive_perm_ranks_opt(Xc::Vector{Matrix{Float64}}, n::Int; nperm::Int,\n\t\t\t\t\t alpha::Float64, conv::Float64, maxiter::Int,\n\t\t\t\t\t maxrounds::Int = 10)\n\nEstimate the joint and individual ranks by a permutation test\n\nArguments\n\nXc::Vector{Matrix{Float64}}: The preprocessed data blocks, each pᵢ×n\nn::Int: The shared number of columns (observations)\nnperm::Int: The number of permutations used to build each null distribution\nalpha::Float64: The significance level; a singular value counts as signal if it exceeds the (1−alpha) quantile of the permuted null\nconv::Float64: The convergence threshold passed to the core fit\nmaxiter::Int: The maximum core-fit iterations\nmaxrounds::Int: The maximum number of estimate-then-refit rounds. Defaults to 10\n\nValue\n\nA tuple (rJ, rA) of the estimated joint rank and the vector of individual ranks. The joint rank is the number of leading singular values of the stacked (data − individual) residual that exceed the column-permuted null; each individual rank is found the same way on each block's (data − joint) residual. Ranks and the fit are alternated until the rank estimates stabilize, following the r.jive permutation procedure of Lock et al. (2013)\n\n\n\n\n\n","title":"BigRiverEssence._jive_perm_ranks_opt"},{"category":"method","location":"api/#BigRiverEssence._jive_rjive_core_opt2-Tuple{Vector{Matrix{Float64}}, Int64, Int64, Vector{Int64}}","page":"API Reference","text":"_jive_rjive_core_opt2(Xc::Vector{Matrix{Float64}}, n::Int, r::Int,\n\t\t\t\t\t  ri::Vector{Int}; conv::Float64, maxiter::Int)\n\nCompute the JIVE decomposition for GIVEN joint and individual ranks, by alternating between estimating the joint and individual structure\n\nArguments\n\nXc::Vector{Matrix{Float64}}: The preprocessed (centered/scaled) data blocks, each pᵢ×n with a shared column dimension n\nn::Int: The number of columns (observations) shared across blocks\nr::Int: The joint rank\nri::Vector{Int}: The individual ranks, one per block\nconv::Float64: The convergence threshold on the change in J and A between iterations\nmaxiter::Int: The maximum number of outer iterations\n\nValue\n\nA jiveStructure with the joint (J, S, U) and individual (A, Si, Wi) parts. Implements the r.jive \"orthIndiv\" algorithm of Lock et al. (2013): wide blocks are first compressed to their score space (via SVD) for efficiency, then the joint structure is the rank-r SVD of the stacked residual (data minus individual), and each block's individual structure is the rank-rᵢ SVD of its residual projected off the joint row space and off the other blocks' individual spaces. The two estimates are alternated to convergence, then mapped back to the original row space\n\n\n\n\n\n","title":"BigRiverEssence._jive_rjive_core_opt2"},{"category":"method","location":"api/#BigRiverEssence._l1_of_norm-Tuple{Any}","page":"API Reference","text":"_l1_of_norm(a)\n\nCompute the ℓ₁/ℓ₂ ratio of a vector\n\nArguments\n\na: 1d array of floats\n\nValue\n\nFloat; ‖a‖₁ / ‖a‖₂. This ratio is the quantity the L1 budget constrains; it ranges from 1 (maximally sparse) to √length(a) (fully dense)\n\n\n\n\n\n","title":"BigRiverEssence._l1_of_norm"},{"category":"method","location":"api/#BigRiverEssence._l1_of_norm_soft-Tuple{Any, Any}","page":"API Reference","text":"_l1_of_norm_soft(a, d)\n\nCompute the ℓ₁/ℓ₂ ratio of a vector AFTER soft-thresholding it by d, without materializing the thresholded vector\n\nArguments\n\na: 1d array of floats; the unthresholded vector\nd: Float; the soft-threshold to apply\n\nValue\n\nFloat; ‖S(a,d)‖₁ / ‖S(a,d)‖₂ where S is the soft-threshold operator. Used inside the binary search to test whether a candidate threshold meets the L1 budget\n\n\n\n\n\n","title":"BigRiverEssence._l1_of_norm_soft"},{"category":"method","location":"api/#BigRiverEssence._l1diff-Tuple{Any, Any}","page":"API Reference","text":"_l1diff(a, b)\n\nCompute the ℓ₁ distance between two vectors\n\nArguments\n\na: 1d array of floats\nb: 1d array of floats\n\nValue\n\nFloat; ‖a − b‖₁. Used as the convergence criterion between successive iterates\n\n\n\n\n\n","title":"BigRiverEssence._l1diff"},{"category":"method","location":"api/#BigRiverEssence._l2n_val-Tuple{Any}","page":"API Reference","text":"_l2n_val(a)\n\nCompute the ℓ₂ norm of a vector with a zero-guard\n\nArguments\n\na: 1d array of floats\n\nValue\n\nFloat; the Euclidean norm ‖a‖₂, or 0.05 when a is all zeros. The guard mirrors PMA's convention and prevents division by zero in the normalization steps\n\n\n\n\n\n","title":"BigRiverEssence._l2n_val"},{"category":"method","location":"api/#BigRiverEssence._matsqrt-Tuple{Any}","page":"API Reference","text":"_matsqrt(A)\n\nCompute the symmetric square root of a symmetric positive-semidefinite matrix\n\nArguments\n\nA: 2d array of floats; a symmetric (positive-semidefinite) matrix\n\nValue\n\n2d array of floats; the symmetric matrix R such that R·R = A, formed from the eigendecomposition with eigenvalues floored at zero (so tiny negative roundoff eigenvalues don't produce complex results)\n\n\n\n\n\n","title":"BigRiverEssence._matsqrt"},{"category":"method","location":"api/#BigRiverEssence._pmd_binary_search-Tuple{Any, Any}","page":"API Reference","text":"_pmd_binary_search(argu, sumabs::Float64)\n\nFind the soft-threshold λ that makes a soft-thresholded vector meet a target L1 budget\n\nArguments\n\nargu: 1d array of floats; the vector to be thresholded\nsumabs::Float64: The target ℓ₁/ℓ₂ ratio (the L1 budget)\n\nValue\n\nFloat; the threshold λ ≥ 0 such that ‖S(argu,λ)‖₁/‖S(argu,λ)‖₂ ≈ sumabs. Returns 0 when the vector already satisfies the budget. Implements the bisection step of Witten, Tibshirani & Hastie (2009), bracketing λ in [0, max|argu|] and halving for up to 150 iterations or until the bracket is below 1e-6\n\n\n\n\n\n","title":"BigRiverEssence._pmd_binary_search"},{"category":"method","location":"api/#BigRiverEssence._pmd_check_v-Tuple{Any, Any}","page":"API Reference","text":"_pmd_check_v(x::Matrix{Float64}, K::Int)\n\nInitialize the right factors from the leading right singular vectors of the data\n\nArguments\n\nx::Matrix{Float64}: 2d array of floats; the (already preprocessed) data matrix\nK::Int: The number of components to initialize\n\nValue\n\n2d array of floats; the p×K matrix of leading right singular vectors of x. Two branches keep the eigenproblem small: for wide data (p > n) it decomposes the n×n Gram matrix x·xᵀ and back-projects, otherwise it decomposes the p×p Gram matrix xᵀ·x directly. SVD initialization is the deterministic starting point recommended in Witten, Tibshirani & Hastie (2009)\n\n\n\n\n\n","title":"BigRiverEssence._pmd_check_v"},{"category":"method","location":"api/#BigRiverEssence._pmd_l1_norm-Tuple{Any}","page":"API Reference","text":"_pmd_l1_norm(a)\n\nCompute the ℓ₁/ℓ₂ ratio of a vector\n\nArguments\n\na: 1d array of floats\n\nValue\n\nFloat; ‖a‖₁ / ‖a‖₂. This ratio is the quantity the L1 budget constrains; it ranges from 1 (maximally sparse) to √length(a) (fully dense)\n\n\n\n\n\n","title":"BigRiverEssence._pmd_l1_norm"},{"category":"method","location":"api/#BigRiverEssence._pmd_l1_norm_soft-Tuple{Any, Any}","page":"API Reference","text":"_pmd_l1_norm_soft(a, λ::Float64)\n\nCompute the ℓ₁/ℓ₂ ratio of a vector AFTER soft-thresholding it by λ, without materializing the thresholded vector\n\nArguments\n\na: 1d array of floats; the unthresholded vector\nλ::Float64: The soft-threshold to apply\n\nValue\n\nFloat; ‖S(a,λ)‖₁ / ‖S(a,λ)‖₂ where S is the soft-threshold operator. Used inside the binary search to test whether a candidate λ meets the L1 budget\n\n\n\n\n\n","title":"BigRiverEssence._pmd_l1_norm_soft"},{"category":"method","location":"api/#BigRiverEssence._pmd_l1diff-Tuple{Any, Any}","page":"API Reference","text":"_pmd_l1diff(a, b)\n\nCompute the ℓ₁ distance between two vectors\n\nArguments\n\na: 1d array of floats\nb: 1d array of floats\n\nValue\n\nFloat; ‖a − b‖₁. Used as the convergence criterion between successive v iterates\n\n\n\n\n\n","title":"BigRiverEssence._pmd_l1diff"},{"category":"method","location":"api/#BigRiverEssence._pmd_l2n-Tuple{Any}","page":"API Reference","text":"_pmd_l2n(a)\n\nCompute the ℓ₂ norm of a vector with a zero-guard\n\nArguments\n\na: 1d array of floats\n\nValue\n\nFloat; the Euclidean norm ‖a‖₂, or 0.05 when a is all zeros. The guard mirrors the R package PMA's convention  and prevents division by zero in the normalization steps\n\n\n\n\n\n","title":"BigRiverEssence._pmd_l2n"},{"category":"method","location":"api/#BigRiverEssence._pmd_smd!-NTuple{10, Any}","page":"API Reference","text":"_pmd_smd!(x, v0, sumabsu::Float64, sumabsv::Float64, niter::Int,\n\t\t  u, v, vold, argu, argv)\n\nCompute one rank-1 sparse factor pair by alternating, soft-thresholded power iteration\n\nArguments\n\nx: 2d array of floats; the (deflated) data matrix for this component\nv0: 1d array of floats; the initial right factor (from _pmd_check_v)\nsumabsu::Float64: The L1 budget on u (in [1, √n])\nsumabsv::Float64: The L1 budget on v (in [1, √p])\nniter::Int: The maximum number of power-iteration steps\nu: 1d array of floats; preallocated buffer for the left factor, written in place\nv: 1d array of floats; preallocated buffer for the right factor, written in place\nvold: 1d array of floats; preallocated scratch holding the previous v iterate\nargu: 1d array of floats; preallocated scratch of length n\nargv: 1d array of floats; preallocated scratch of length p\n\nValue\n\nFloat; the weight d = uᵀ x v for the fitted component. On return u and v hold the unit-ℓ₂, L1-penalized factors. Each step alternates u ← normalize(S(x v, λᵤ)) and v ← normalize(S(xᵀ u, λᵥ)), with λ chosen by _pmd_binary_search to meet the budgets; this is the single-factor PMD(L1,L1) update of Witten, Tibshirani & Hastie (2009). The randn primer on vold only forces the first iteration to run and does not affect the converged fixed point\n\n\n\n\n\n","title":"BigRiverEssence._pmd_smd!"},{"category":"method","location":"api/#BigRiverEssence._pmd_soft-Tuple{Any, Any}","page":"API Reference","text":"_pmd_soft(a::Float64, λ::Float64)\n\nSoft-threshold a scalar: shrink toward zero by λ, clamping at zero\n\nArguments\n\na::Float64: The value to threshold\nλ::Float64: The nonnegative threshold\n\nValue\n\nFloat; sign(a)·max(|a| − λ, 0). The elementwise solution to the L1-penalized update in Witten, Tibshirani & Hastie (2009)\n\n\n\n\n\n","title":"BigRiverEssence._pmd_soft"},{"category":"method","location":"api/#BigRiverEssence._pmd_soft_normalize!-Tuple{Any, Any, Any}","page":"API Reference","text":"_pmd_soft_normalize!(out, arg, λ::Float64)\n\nSoft-threshold a vector by λ and rescale it to unit ℓ₂ norm, writing into a preallocated buffer\n\nArguments\n\nout: 1d array of floats; the destination buffer, overwritten in place\narg: 1d array of floats; the vector to threshold\nλ::Float64: The soft-threshold\n\nValue\n\nThe buffer out, holding S(arg,λ) normalized to unit ℓ₂ norm (or all zeros, guarded against division by zero). This is the factor update of the PMD power iteration\n\n\n\n\n\n","title":"BigRiverEssence._pmd_soft_normalize!"},{"category":"method","location":"api/#BigRiverEssence._qnormalize!-Tuple{Any, Any}","page":"API Reference","text":"_qnormalize!(P, C)\n\nNormalize the columns of a direction matrix to unit variance under a covariance metric, in place\n\nArguments\n\nP: 2d array of floats; the directions (columns), overwritten in place\nC: 2d array of floats; the covariance defining the metric\n\nValue\n\nThe matrix P, with each column pⱼ rescaled so that pⱼᵀ C pⱼ = 1 (unit variance of the corresponding canonical variate). Used to normalize the recovered side's directions in the covariance-based CCA solver\n\n\n\n\n\n","title":"BigRiverEssence._qnormalize!"},{"category":"method","location":"api/#BigRiverEssence._soft_threshold_L1!-Tuple{Any, Any, Int64, Any, Any, Any}","page":"API Reference","text":"_soft_threshold_L1!(out, x, nx::Int, absx, ord, ranks)\n\nSoft-threshold a loading vector to keep only its largest-magnitude entries, writing the result into a preallocated buffer\n\nArguments\n\nout: 1d array of floats; the destination buffer, overwritten in place\nx: 1d array of floats; the loading vector to threshold\nnx::Int: The number of SMALLEST-magnitude entries to drop (= p − keepX). If nx ≤ 0 the vector is copied through unchanged\nabsx: 1d array of floats; preallocated scratch for the entry magnitudes\nord: 1d array of ints; preallocated scratch for the sort permutation\nranks: 1d array of ints; preallocated scratch for the magnitude ranks\n\nValue\n\nThe buffer out, holding the thresholded vector: the keepX = p − nx largest entries survive (shrunk by λ = the largest dropped magnitude), the rest are zeroed. Implements the L1 penalty Pλ(u) = sign(u)(|u| − λ)₊ of Lê Cao et al. (2011), reparameterized as a keep-count rather than a λ (mixOmics `softthresholding_L1`). The rank computation handles magnitude ties so that tied entries are dropped or kept together\n\n\n\n\n\n","title":"BigRiverEssence._soft_threshold_L1!"},{"category":"method","location":"api/#BigRiverEssence._softcca!-Tuple{Any, Any, Any}","page":"API Reference","text":"_softcca!(out, a, d)\n\nSoft-threshold a vector into a preallocated buffer\n\nArguments\n\nout: 1d array of floats; the destination buffer, overwritten in place\na: 1d array of floats; the vector to threshold\nd: Float; the nonnegative threshold\n\nValue\n\nThe buffer out, holding sign(aᵢ)·max(|aᵢ| − d, 0). The elementwise L1-penalized update of Witten, Tibshirani & Hastie (2009)\n\n\n\n\n\n","title":"BigRiverEssence._softcca!"},{"category":"method","location":"api/#BigRiverEssence._sparse_cca_single_opt!-NTuple{15, Any}","page":"API Reference","text":"_sparse_cca_single_opt!(u, v, x, z, v0, penaltyx, penaltyz, niter,\n\t\t\t\t\t\tvold, zv_buf, xu_buf, argu, argv, su, sv)\n\nCompute one rank-1 sparse canonical vector pair by alternating, soft-thresholded power iteration\n\nArguments\n\nu: 1d array of floats; the X canonical vector, written in place\nv: 1d array of floats; the Z canonical vector, written in place\nx: 2d array of floats; the (observations × features) X block for this component\nz: 2d array of floats; the (observations × features) Z block for this component\nv0: 1d array of floats; the initial v (from _fast_init_v or an SVD)\npenaltyx: Float; the X penalty in (0,1], scaled internally to the budget penaltyx·√(#X features)\npenaltyz: Float; the Z penalty in (0,1], scaled to penaltyz·√(#Z features)\nniter: Int; the maximum number of power-iteration steps\nvold: 1d array of floats; scratch holding the previous v iterate\nzv_buf, xu_buf: 1d arrays of floats; scratch for z·v and x·u (sized for the deflated block height)\nargu, argv: 1d arrays of floats; scratch for the raw (pre-threshold) canonical vectors\nsu, sv: 1d arrays of floats; scratch for the soft-thresholded vectors\n\nValue\n\nFloat; the weight d = uᵀ xᵀ z v = ⟨x u, z v⟩. On return u and v hold the unit-ℓ₂, L1-penalized canonical vectors. Each step alternates u ← normalize(S(xᵀz v, λᵤ)) and v ← normalize(S(zᵀx u, λᵥ)), with the thresholds chosen by _binary_search_opt to meet the penalty budgets — the penalized-CCA update of Witten, Tibshirani & Hastie (2009). The randn primer on vold only forces the first iteration to run and does not affect the converged fixed point\n\n\n\n\n\n","title":"BigRiverEssence._sparse_cca_single_opt!"},{"category":"method","location":"api/#BigRiverEssence._sqdiff-Tuple{Any, Any}","page":"API Reference","text":"_sqdiff(a, b)\n\nCompute the squared ℓ₂ distance between two vectors\n\nArguments\n\na: 1d array of floats\nb: 1d array of floats\n\nValue\n\nFloat; Σ(aᵢ − bᵢ)². Used as the convergence criterion between successive loading iterates\n\n\n\n\n\n","title":"BigRiverEssence._sqdiff"},{"category":"method","location":"api/#BigRiverEssence._unmap-Tuple{AbstractVector}","page":"API Reference","text":"_unmap(y::AbstractVector; levels = nothing)\n\nOne-hot encode a vector of class labels into a dummy indicator matrix\n\nArguments\n\ny::AbstractVector: The class labels, one per observation\nlevels: An optional ordering of the classes; when given, it fixes the column order of the output and must list each class exactly once. Defaults to nothing (classes sorted by sort(unique(y)))\n\nValue\n\nA tuple (Yd, classes) where Yd is the n×K one-hot matrix (Yd[i,k] = 1 when observation i is in class k, else 0) and classes is the class labels in column order. This is mixOmics' unmap, the dummy encoding that turns classification into a PLS regression onto the indicator matrix\n\n\n\n\n\n","title":"BigRiverEssence._unmap"},{"category":"method","location":"api/#BigRiverEssence.finding_v!-NTuple{4, Any}","page":"API Reference","text":"finding_v!(v, s, z, c)\n\nProject a vector onto the intersection of the unit ℓ₂ ball and the L1 budget, writing the result in place\n\nArguments\n\nv: 1d array of floats; the destination buffer for the result, overwritten in place\ns: 1d array of floats; preallocated scratch for the soft-thresholded vector\nz: 1d array of floats; the raw direction to project (e.g. Xᵀu)\nc: Float; the L1 budget (in [1, √p]); the returned v satisfies ‖v‖₂ = 1 and ‖v‖₁ ≤ c\n\nValue\n\nThe buffer v, holding the unit-ℓ₂ vector closest to z whose ℓ₁ norm meets the budget. If the plain unit-normalized z already satisfies the budget it is returned unchanged; otherwise a soft-threshold δ is found by bisection (up to 150 steps) so that the normalized, thresholded vector has ‖v‖₁ ≈ c. This is the penalized update of the SPC criterion in Witten, Tibshirani & Hastie (2009)\n\n\n\n\n\n","title":"BigRiverEssence.finding_v!"},{"category":"method","location":"api/#BigRiverEssence.init_rsv-Tuple{Any, Any}","page":"API Reference","text":"init_rsv(Xc, k::Int)\n\nCompute the top-k right singular vectors of a centered data matrix, used to initialize the sparse power iteration\n\nArguments\n\nXc: 2d array of floats; the centered (and optionally scaled) data matrix\nk::Int: The number of leading right singular vectors to return\n\nValue\n\n2d array of floats; the p×k matrix of leading right singular vectors of Xc. Two branches keep the eigenproblem small: for tall data (p ≤ n) it eigendecomposes the p×p Gram matrix XcᵀXc directly, otherwise it eigendecomposes the n×n Gram matrix XcXcᵀ and back-projects. Both use a partial eigendecomposition (only the top k eigenpairs) and return columns in descending eigenvalue order\n\n\n\n\n\n","title":"BigRiverEssence.init_rsv"},{"category":"method","location":"api/#BigRiverEssence.l1_diff-Tuple{Any, Any}","page":"API Reference","text":"l1_diff(a, b)\n\nCompute the ℓ₁ distance between two vectors\n\nArguments\n\na: 1d array of floats\nb: 1d array of floats\n\nValue\n\nFloat; ‖a − b‖₁. Used as the convergence criterion between successive iterates\n\n\n\n\n\n","title":"BigRiverEssence.l1_diff"},{"category":"method","location":"api/#BigRiverEssence.prop_var_explained-Tuple{Any, Any}","page":"API Reference","text":"prop_var_explained(Xc, V)\n\nCompute the cumulative proportion of variance explained by sparse components\n\nArguments\n\nXc: 2d array of floats; the centered data matrix\nV: 2d array of floats; the p×K matrix of (possibly non-orthonormal) sparse loadings\n\nValue\n\n1d array of floats of length K; entry k is the proportion of total variance explained by the first k loadings jointly. Because sparse loadings need not be orthonormal, the adjusted variance uses the projection ‖Xc·Vₖ(VₖᵀVₖ)⁻¹Vₖᵀ‖²F / ‖Xc‖²F, evaluated here in the equivalent trace form tr((VₖᵀVₖ)⁻¹ VₖᵀXcᵀXcVₖ) / ‖Xc‖²_F. This is the adjusted-variance measure of Shen & Huang / Witten et al. for non-orthogonal sparse PCs\n\n\n\n\n\n","title":"BigRiverEssence.prop_var_explained"},{"category":"method","location":"api/#BigRiverEssence.safe_svd!-Tuple{Any}","page":"API Reference","text":"safe_svd!(A)\n\nIn-place variant of safe_svd: compute an SVD, overwriting A, with a robust fallback on convergence failure\n\nArguments\n\nA: 2d array of floats; the matrix to decompose, OVERWRITTEN in place by the default path\n\nValue\n\nAn SVD factorization object. Tries the in-place svd! first; on a LAPACKException retries with the (non-mutating) QR-iteration SVD. Used in the inner JIVE iterations where the input is scratch that can be destroyed\n\n\n\n\n\n","title":"BigRiverEssence.safe_svd!"},{"category":"method","location":"api/#BigRiverEssence.safe_svd-Tuple{Any}","page":"API Reference","text":"safe_svd(A)\n\nCompute an SVD, falling back to a slower but more robust algorithm if the default one fails to converge\n\nArguments\n\nA: 2d array of floats; the matrix to decompose\n\nValue\n\nAn SVD factorization object. Tries the default divide-and-conquer SVD first; if it raises a LAPACKException (a convergence failure on certain ill-conditioned inputs), retries with the QR-iteration algorithm, which is slower but more stable. Any other error is rethrown\n\n\n\n\n\n","title":"BigRiverEssence.safe_svd"},{"category":"method","location":"api/#BigRiverEssence.safe_svdvals-Tuple{Any}","page":"API Reference","text":"safe_svdvals(A)\n\nCompute singular values, falling back to a more robust algorithm if the default one fails to converge\n\nArguments\n\nA: 2d array of floats; the matrix whose singular values are wanted\n\nValue\n\nA vector of singular values. Like safe_svd, retries with QR iteration on a LAPACKException and rethrows anything else\n\n\n\n\n\n","title":"BigRiverEssence.safe_svdvals"},{"category":"method","location":"api/#BigRiverEssence.sign_consistency_opt!-Tuple{Any}","page":"API Reference","text":"sign_consistency_opt!(V)\n\nFix the sign of each column of a loading matrix so the largest-magnitude entry is positive, in place\n\nArguments\n\nV: 2d array of floats; the loading/direction matrix whose columns are sign-fixed in place\n\nValue\n\nThe matrix V, with each column multiplied by ±1 so that its entry of largest absolute value is positive. SVD- and eigen-derived directions are only determined up to a sign, so this canonicalizes the choice to make results reproducible across runs and comparable across implementations. An all-zero column (whose largest entry has sign 0) is left untouched\n\n\n\n\n\n","title":"BigRiverEssence.sign_consistency_opt!"},{"category":"method","location":"api/#BigRiverEssence.spca_component!-NTuple{8, Any}","page":"API Reference","text":"spca_component!(v, X, c, u, Xv, Xtu, s, vold; tol = 1e-7, niter = 20)\n\nCompute one sparse principal component by soft-thresholded power iteration (deflation variant)\n\nArguments\n\nv: 1d array of floats; the right factor, initialized by the caller and overwritten in place with the converged loading\nX: 2d array of floats; the (deflated) data matrix for this component\nc: Float; the L1 budget on v (in [1, √p])\nu: 1d array of floats; preallocated buffer for the left factor (scores direction)\nXv: 1d array of floats; preallocated scratch of length n\nXtu: 1d array of floats; preallocated scratch of length p\ns: 1d array of floats; preallocated scratch for finding_v!\nvold: 1d array of floats; preallocated scratch holding the previous v iterate\ntol: Float; the ℓ₁ convergence tolerance on v. Defaults to 1e-7\nniter::Int: The maximum number of power-iteration steps. Defaults to 20\n\nValue\n\nFloat; the component weight d = uᵀ X v. On return v holds the unit-ℓ₂, L1-penalized loading and u the corresponding unit-ℓ₂ scores direction. Each step sets u ← normalize(X v) then v ← project(Xᵀ u) onto the unit ball and L1 budget via finding_v!, the SPC update of Witten, Tibshirani & Hastie (2009)\n\n\n\n\n\n","title":"BigRiverEssence.spca_component!"},{"category":"method","location":"api/#BigRiverEssence.spca_component_orth!-NTuple{11, Any}","page":"API Reference","text":"spca_component_orth!(v, X, c, U_prev, u, uold, Xv, Xtu, s, vold, proj;\n\t\t\t\t\t tol = 1e-6, niter = 20)\n\nCompute one sparse principal component whose scores are orthogonal to all previously extracted components\n\nArguments\n\nv: 1d array of floats; the right factor, initialized by the caller and overwritten in place with the converged loading\nX: 2d array of floats; the (uncentered-residual) data matrix — note the orthogonal variant decomposes the original Xc, not a deflated residual\nc: Float; the L1 budget on v (in [1, √p])\nU_prev: 2d array of floats; the n×(j-1) matrix of previously extracted scores directions that the new u must be orthogonal to (empty for the first component)\nu: 1d array of floats; preallocated buffer for the left factor, written in place\nuold: 1d array of floats; preallocated scratch holding the previous u iterate\nXv: 1d array of floats; preallocated scratch of length n\nXtu: 1d array of floats; preallocated scratch of length p\ns: 1d array of floats; preallocated scratch for finding_v!\nvold: 1d array of floats; preallocated scratch holding the previous v iterate\nproj: 1d array of floats; preallocated scratch for the orthogonal projection coefficients\ntol: Float; the ℓ₁ convergence tolerance on u and v. Defaults to 1e-6\nniter::Int: The maximum number of power-iteration steps. Defaults to 20\n\nValue\n\nFloat; the component weight d = uᵀ X v. Differs from spca_component! by projecting u onto the orthogonal complement of U_prev each iteration (u ← u − Uprev Uprevᵀ u before normalizing), which enforces orthogonal scores across components instead of relying on deflation — the orthogonal SPC variant of Witten, Tibshirani & Hastie (2009)\n\n\n\n\n\n","title":"BigRiverEssence.spca_component_orth!"},{"category":"section","location":"pmd_tutorial/#PMD:-Penalized-Matrix-Decomposition","page":"Penalized Matrix Decomposition","text":"Penalized Matrix Decomposition or PMD is a matrix decomposition technique which is used for factoring a data matrix into small number of ranked one matrices along with a L_1 penalization that enforces most of the loadings to be 0.  \n\nIn Singular Value Decomposition or SVD, a matrix X is represented as a sum of ranked one matrices as X = Sigma d_k u_k v_k where u_k and v_k are orthonormal singular vectors and d_k are singular values. In PMD, this ranked one representation can be transformed into an optimization problem by considering unit vectors u and v which maximize u X v while imposing constraints on u and v. Hence, in PMD, we consider the following optimization problem: max u X v text subject to the constraint   u_2 le 1 text v_2 le 1 text P_1(u) le c_1text P_2(v) le c_2 We choose the penalties P_1 and P_2 to be L1 (lasso) penalties. This produces sparsity as L_1 budges makes many entries u and v to be exavtly 0 making each components to be supported ny only a few observations and variables. The parameters in pmd: sumabs (or sumabsu, sumabsv) set how large the L_1 norm of each factor will be where smaller budgets give sparser factors.\n\nThe optimization problem mentioned above is solved by an alternating algorithm where we keep v fixed first and solve for u using a soft thresholding step. Then we solve for v by holding u fixed. Additional components are obtained in a similar fashion as done in SVD where we remove the fitted ranked one piece from X and repeat on the residual.\n\nIn this documentation, we will depomstrate implementation of PMD using BigRiverEssence.pmd on a BigRiverEssenceast cancer dataset. This dataset has been taken from ","title":"PMD: Penalized Matrix Decomposition"},{"category":"section","location":"pmd_tutorial/#The-data","page":"Penalized Matrix Decomposition","text":"The dataset used in this study contains DNA copy-number measurements at 2149 genomic locations (CGH spots) across 89 BigRiverEssenceast cancer samples along with chromosome and position of each spot. The dataset is obtained via the PMA R package, where it serves as the worked example for the penalized matrix decomposition of Witten, Tibshirani & Hastie (2009) [2].\n\n\nusing BigRiverEssence\nusing Plots, Statistics, DelimitedFiles\n\n\ndatadir = joinpath(pkgdir(BigRiverEssence), \"reference_Data\", \"breastdata\") \ndna   = readdlm(joinpath(datadir, \"dna.csv\"),   ',', Float64)   # 89 × 2149\nchrom = Int.(vec(readdlm(joinpath(datadir, \"chrom.csv\"), ',')))  # 2149\nnuc   = vec(readdlm(joinpath(datadir, \"nuc.csv\"), ',', Float64)) # 2149\nsize(dna), length(chrom)","title":"The data"},{"category":"section","location":"pmd_tutorial/#The-method-summary","page":"Penalized Matrix Decomposition","text":"We will center the data matrix (and can optionally standardize it).\nWe will then find a rank-one approximation of the matrix, essentially a pair of factors u and v. Both of them will have L_1 penalty. This will force most of their entries to be 0. This will make the factors sparse where each component will use only a few observations and a few variables.\nThe strength of the penalty is set by sumabs (smaller values give sparser factors), and the factors are found by an alternating soft-thresholding algorithm that updates u and v in turn until they converge.\nTo obtain further components, we remove the fitted rank-one piece from the matrix and repeat on what remains (deflation), just as the SVD builds up a matrix one rank-one piece at a time.","title":"The method summary"},{"category":"section","location":"pmd_tutorial/#Fitting-the-model","page":"Penalized Matrix Decomposition","text":"Now we fit the pmd function to the dna matrix which has 89 samples × 2149 genomic spots. The argument sumabs = 0.4 sets the sparsity budget. Here we consider a single ranked one component, hence we set K = 1. We use center = true that subtracts the grand mean before decomposing.\n\nm = pmd(dna; sumabs = 0.4, K = 1, center = true)\n\nThe result in m containts the sparse left and right factors (m.u, m.v) and the weight m.d. The right factor m.v is a 2149 long loading vector containing one value per genomic spot where most of them exactly are 0.","title":"Fitting the model"},{"category":"section","location":"pmd_tutorial/#The-sparse-copy-number-signature","page":"Penalized Matrix Decomposition","text":"Each of the 2149 genomic spots which are our variables gets one loading value in m.v which are its weight in the sparse component. Since L_1 penalty of PMD drives most of those to 0, the loading vector is mostly zeros with a small number of nonzero values. To get an idea of where those nonzero entries are falling, we can plot each spot's loading against its ordering along the genome (where spots are arranged chromosome by chromosome, position by position), and color each point by the chromosome it belongs to. The result will be a genome wide view where a flat line of zeros will be interrupted by clusters of nonzero loadings, each cluster marking a genomic region that PMD selected as varying across the samples.\n\nv = m.v[:, 1]\n# find where each chromosome starts, to place labels\nchrom_ids = unique(chrom)\ntick_pos = [findfirst(==(c), chrom) for c in chrom_ids]\n\nscatter(1:length(v), v; group = chrom, markersize = 2, legend = false,\n    xticks = (tick_pos, string.(chrom_ids)), xrotation = 90,\n    xlabel = \"chromosome\", ylabel = \"PMD loading\",\n    title = \"Sparse copy-number signature (breast cancer, PMD)\")\nhline!([0], color = :black, alpha = 0.3)\n\nWith genomic position on the x-axis, each nonzero loading marks a spot where the copy number changes together across samples. The nonzero spots cluster into a contiguous stretch rather than scattering randomly. That contiguity is the signature of a real copy-number alteration, since gains and losses affect whole chromosomal segments at once, not isolated points. The zeroed spots on either side are the regions PMD considered uninvolved.","title":"The sparse copy-number signature"},{"category":"section","location":"pmd_tutorial/#Summary","page":"Penalized Matrix Decomposition","text":"Here we demonstrated the use of PMD using the pmd function from the Julia package BigRiverEssence.jl. PMD can also be applied to any similar dataset.","title":"Summary"},{"category":"section","location":"pmd_tutorial/#References","page":"Penalized Matrix Decomposition","text":"[1] Chin, K., DeVries, S., Fridlyand, J., et al. (2006). Genomic and transcriptional     aberrations linked to breast cancer pathophysiologies. Cancer Cell, 10, 529–541.     doi:10.1016/j.ccr.2006.10.009\n\n[2] Witten, D. M., Tibshirani, R., & Hastie, T. (2009). A penalized matrix decomposition,     with applications to sparse principal components and canonical correlation analysis.     Biostatistics, 10(3), 515–534. doi:10.1093/biostatistics/kxp008","title":"References"},{"category":"section","location":"spc_tutorial/#SPC:-Sparse-Principal-Component-Analysis","page":"Sparse Principal Component Analysis","text":"Sparse Principal Component Analysis or SPC can be considered as a variant of principal component analysis or PCA that, similar to PCA, finds the directions of greatest variance in a data matrix while enforcing an L_1 penalty on the loadings. Thus ensures that each component is supported by only a few variables.\n\nIn PCA, the principal components gives the direction of maximum variability of the centered data. Equivalently, the leading component solves max u X v over unit vectors u and v. Here every variable receives a nonzero loading. SPC keeps this variance maximizing criterion of PCA but adds a penalty on the loading vector v only. This differntiates with with Penalized matrix decomosition which adds penalty at both, u and v. In SPC, we consider the following optimization problem: max u X v text subject to the constraint  u_2 le 1 text  v_2 le 1 text  P(v) le c We consider the penalty P to be an l_1(lasso) penalty. This produces sparsity as the L_1 budget forces many entries of v to be exactly 0, so each component is defined by only a small number of variables. The parameter in spc: c sets how large the L_1 norm of each loading will be in a range of 1 sqrtp. Here smaller values will give sparser loadings and c = sqrtp will recover PCA.\n\nThe optimization problem is solved by an alternating algorithm where we hold v fixed and then set u leftarrow Xv (normalized). Then we hold u fixed and solve for v by soft-thresholding Xu into the L_1 budget. We remove the additional components in spc by removing the fitted rank-one piece from X and repeating them on the residual. This in the deflation of SVD. In spc_orth,we extract each component from the original centered matrix. We force its scores u to be orthogonal to all previously extracted components. This provides us with an orthonormal set of score direction.\n\nIn this documentation, we will demonstrate implementation of SPC using BigRiverEssence.spc and BigRiverEssence.spc_orth on a breast cancer dataset.","title":"SPC: Sparse Principal Component Analysis"},{"category":"section","location":"spc_tutorial/#The-method-summary","page":"Sparse Principal Component Analysis","text":"We will center the data matrix (and can optionally standardize it to unit variance).\nWe will then find the leading principal directions, which are  the axes of the largest variance. However, unlike in PCA, we will consider an L1 penalty on each loading. This will forces most of its entries to be 0. Hence we will gte each components to be described by only a few variables. \nThe penalty strength is set by c which is essentially the L_1 budget in 1 sqrtp. Here a smaller c will give sparser loadings whereas c = sqrtp will recover ordinary  PCA.\nWe will report the variance each sparse component carries and the cumulative proportion of total variance the components explain together.\n\nThis package provides two variants: spc, which extracts components by deflation, and spc_orth, which constrains the component scores to be mutually orthogonal.","title":"The method summary"},{"category":"section","location":"spc_tutorial/#The-data","page":"Sparse Principal Component Analysis","text":"We use the same breast cancer copy-number matrix as we used in the demonstration of  PMD. The matrix contains 89 samples, each with 2149 genomic spots. The dataset is obtained via the PMA R package, where it serves as the worked example for the penalized matrix decomposition of Witten, Tibshirani & Hastie (2009) [2].  Here genomic spots are variables. We note that, here the number of variables are far larger than the number of samples. Using naive PCA will lead to components or directions each with all the variables, this will lead to probles involving over parameterization. Hence, this dataset is well suited for SPC which will consider only a few number of variables per components. \n\nusing BigRiverEssence\nusing  Plots, Statistics, DelimitedFiles\n\n\ndatadir = joinpath(pkgdir(BigRiverEssence), \"reference_Data\", \"breastdata\")\ndna   = readdlm(joinpath(datadir, \"dna.csv\"),   ',', Float64)   # 89 × 2149\nchrom = Int.(vec(readdlm(joinpath(datadir, \"chrom.csv\"), ',')))  # 2149\nsize(dna)","title":"The data"},{"category":"section","location":"spc_tutorial/#Fitting-SPC","page":"Sparse Principal Component Analysis","text":"The function spc fits the deflation variant. We will consider 3 components and will set k = 3. We will set the L_1budget c way below sqrtp to enforce sparsity. We will standardize so every variable is on equal footing.\n\nc = sqrt(size(dna, 2)) / 4          # a fairly tight sparsity budget\nm = spc(dna; k = 3, c = c, standardize = true)","title":"Fitting SPC"},{"category":"section","location":"spc_tutorial/#How-much-variance,-and-how-sparse?","page":"Sparse Principal Component Analysis","text":"The main goal of SPC is to  capture variance with few variables only. We can quantify both using propOFvar and loadings of spc.\n\n@show m.propOFvar[end]\n@show [count(!iszero, m.loadings[:, j]) for j in 1:3]\n@show [count(!iszero, m.loadings[:, j]) / size(dna, 2) for j in 1:3]\n\nWe can see from the above output, the three sparse components together explain about 8 of the total variance, while each uses only about 913 of the 2149 genomic spots. Every other loading is made 0.\n\nTo get an idea of what the sparsity costs we can compare with ordinary PCA which we obtain by setting the sparsity budget to its maximum value c = √p:\n\nm_dense = spc(dna; k = 3, c = sqrt(size(dna, 2)), standardize = true)\nm_dense.propOFvar[end]\n\nWe see that ordinary or dense PCA explains about 175 with three components while using all the 2149 variables in each. SPC recovers little bit less than half of that variance while throwing away roughly 90 of the variables. This shows that, when the signal is concentrated only at a few variables, SPC costs very less and when variation is spread across several variables, sparsity gives up more variance in return for components that are far easier to interpret. The budget c lets us decide on this trade off. ","title":"How much variance, and how sparse?"},{"category":"section","location":"spc_tutorial/#Visualizing-a-sparse-component","page":"Sparse Principal Component Analysis","text":"In SPC, each loading of each component is sparse. Hence, if we plot it along the genome, it will show which regions defines that principal direction. We can make a scatter plot of the two loadings to visualize this.\n\nscatter(1:size(m.loadings, 1), m.loadings[:, 1]; group = chrom,\n    markersize = 2, legend = false,\n    xlabel = \"genomic spot\", ylabel = \"loading (PC1)\",\n    title = \"First sparse principal component\")\nhline!([0], color = :black, alpha = 0.3)\n\nThe first sparse principal component concentrates its loadings in a few genomic regions — a strong cluster around spots 250–350, another near 600, and scattered weaker regions elsewhere — while the large majority of the 2149 spots have loadings of exactly zero (the flat line). So the leading axis of variation in this copy-number data is described by a small set of chromosomal regions rather than by all the spots at once. The colors mark chromosomes, so each cluster can be read off to the chromosome it sits on.","title":"Visualizing a sparse component"},{"category":"section","location":"spc_tutorial/#The-orthogonal-variant:-spc_orth","page":"Sparse Principal Component Analysis","text":"The spc function extracts components by deflation. This cannot does enforce orthogonality of the component scores. spc_orth, on the other hand, adds this constraint where it ensures each component's scores are orthogonal to all previous one. This gives us an orthonormal set of score directions. Similar to spc, in spc_orth as well, we can get the proportion of variance explained with the few selected varibles and the loadings using propOFvar and loadings respectively. \n\nmo = spc_orth(dna; k = 3, c = c, standardize = true)\n@show mo.propOFvar[end]                                  # cumulative variance explained\n@show [count(!iszero, mo.loadings[:, j]) for j in 1:3]  # number of non-zero loadings per component\n\nspc_orth returns the same spcStructure type, so we can read its fields exactly as before. We can use spc when we want the simplest sparse components, and spc_orth when orthogonal scores matter for downstream interpretation.","title":"The orthogonal variant: spc_orth"},{"category":"section","location":"spc_tutorial/#Summary","page":"Sparse Principal Component Analysis","text":"spc and  spc_orthis most suitable for wide matrices where the number of variables or columns are more than number of observations or rows. They noth produced a few sparse principal components that capture much of the variance while each depending on only a small set of variables. spc_orth offers in addion, orthogonal scores. Here we demonstrated the use of SPC using spc and spc_orth functions from the Julia package BigRiverEssence.jl. SPC can also be applied to any similar dataset.","title":"Summary"},{"category":"section","location":"spc_tutorial/#References","page":"Sparse Principal Component Analysis","text":"[1] Chin, K., DeVries, S., Fridlyand, J., et al. (2006). Genomic and transcriptional     aberrations linked to breast cancer pathophysiologies. Cancer Cell, 10, 529–541.     doi:10.1016/j.ccr.2006.10.009\n\n[2] Witten, D. M., Tibshirani, R., & Hastie, T. (2009). A penalized matrix decomposition,     with applications to sparse principal components and canonical correlation analysis.     Biostatistics, 10(3), 515–534. doi:10.1093/biostatistics/kxp008","title":"References"},{"category":"section","location":"pca_tutorial/#PCA:-Principal-Component-Analysis","page":"Principal Component Analysis","text":"Principal Component Analysis or PCA points is a dimension reduction technique that gives the direction that explains the most variability in a dataset. It is mostly used in exploratory data analysis and visualization. \n\nSuppose we have a data matrix with many correlated variables. Using naive regression techniques in such a setting can lead to problems such as multicolinearity and curse of dimensionality. PCA constructs small number of ordered uncorrelated axes, which are called principal components, such that the first captures the most variance in the dataset. each principal component follows the previous one in amount of variablity explained. Projecting the data onto first few principal components reveals pattern or structure which are hidden in the original dataset or matrix. \n\nIn this documentation, we will depomstrate implementation of PCA using BigRiverEssence.pca on the well known R dataset, iris. We will then visualize the results using a scree plot, a principal-component scatter plot, and a biplot. ","title":"PCA: Principal Component Analysis"},{"category":"section","location":"pca_tutorial/#The-Dataset","page":"Principal Component Analysis","text":"The iris dataset is contains 4 flower features (sepal length, sepal width, petal length, petal width) for 150 plants. The plants are classified as one of the three species: Setosa, Versicolor, and Virginica. The iris dataset can be considered as a matrix with n=150 rows and p=4 variables, one for each feature.\n\nIn the function pca, observations needs to be in rows and the variables should be in column. Hence, we first get the iris dataset from R and construct a 150 times 4 matrix.\n\nusing BigRiverEssence\nusing RDatasets, DataFrames, Plots, Statistics, LinearAlgebra\n\niris = dataset(\"datasets\", \"iris\")\nfeatures = [:SepalLength, :SepalWidth, :PetalLength, :PetalWidth]\nX = Matrix{Float64}(iris[:, features])\nspecies = iris.Species           \nsize(X) ","title":"The Dataset"},{"category":"section","location":"pca_tutorial/#The-method-summary","page":"Principal Component Analysis","text":"We will center each variable (and can optionally scales it to unit variance).\nWe will then finds the the principal components which are the eigenvectors of the covariance matrix obtained as the right singular vectors of the centered data.\nWe will then project the observations onto the directions of the principal components to obtain scores. We can then report  how much of the total variance are explained by each of the principal components.","title":"The method summary"},{"category":"section","location":"pca_tutorial/#Fitting-the-model","page":"Principal Component Analysis","text":"Measurements of the different columns can be at different scales, hence it is always advisible to standardize each variables to unit variance. We fit pca on the data matrix X while considering standardized columns of X. We can get the proportion of total variance explained by each of the four principal components using propOFvar. \n\nm = BigRiverEssence.pca(X; standardize = true)\nm.propOFvar\n\nWe can see that the first component explains around 73 of the total varinace of X and the first two components together explains around  96. Hence We can say that the first two components should suffice in summarizing the iris data. ","title":"Fitting the model"},{"category":"section","location":"pca_tutorial/#Scree-plot","page":"Principal Component Analysis","text":"A scree plot is just a bar plot where each bar shows the variance captured by each individual component. The \"elbow\" in the scree plot is a good indicator that can help decide how many components are worth keeping. We can use propOFvar to get the scree plot.\n\npve = m.propOFvar\nbar(pve .* 100;\n    xlabel = \"Principal component\", ylabel = \"% of variance explained\",\n    title = \"Scree plot iris dataset\", legend = false, xticks = 1:length(pve))\n\nWe can see that the first bar dominates. A good reason for this can be that the iris measurements are highly correlated, hence a single axis or principal component can captures most of the variation.","title":"Scree plot"},{"category":"section","location":"pca_tutorial/#Principal-component-scatterplot","page":"Principal Component Analysis","text":"Since it is obvious from the scree plot above, thate the first two principal component dominates. We can now project X onto  the first two principal components using the function pca_transform. This will yeild us a two-dimensional score. We make a scatter plot of the two score colouring the observations by their species. \n\nProjecting the flowers onto the first two principal components gives each one a two-dimensional score. Coloring by species reveals what PCA found.\n\n\nscores = pca_transform(m, X)          # 150×4 scores\nscatter(scores[:, 1], scores[:, 2]; group = species,\n    xlabel = \"PC1\", ylabel = \"PC2\",\n    title = \"iris using first two  principal components\",\n    legend = :topright, markeralpha = 0.7)\n\nWe can see in the above plot, the first two components were able to completely separate setosa and were able to split versicolor* from virginica with only mild overlap. ","title":"Principal-component scatterplot"},{"category":"section","location":"pca_tutorial/#Biplot","page":"Principal Component Analysis","text":"After fitting pca, we can get loadings using loadings. Loadings are the weights which says how much each original variable contributes to a particular component.\n\nA biplot iscatterplot of the scores from the first two principal components overlayed with the loadings (as seen as arrows). Now we remake the scatterplot with arrows denoting the loadings. \n\nL = m.loadings\narrowscale = 3.0\n\nplt = scatter(scores[:, 1], scores[:, 2]; group = species,\n    xlabel = \"PC1\", ylabel = \"PC2\", title = \"iris biplot\",\n    legend = :topright, markeralpha = 0.4,\n    top_margin = 8Plots.mm)       \nfor j in 1:length(features)\n    quiver!([0], [0], quiver = ([L[j,1]*arrowscale], [L[j,2]*arrowscale]); color = :black)\n    annotate!(L[j,1]*arrowscale*1.15, L[j,2]*arrowscale*1.15,\n              text(string(features[j]), 8, :black, :left))\nend\nplt\n\nWe can see in the biplot that the petal-length and petal-width arrows are pointing  strongly along PC1. This shows that the petal size is the dominant source of variation and is the dimension along which the species separate. Since the sepal measurements points in other direction than PC1, they contribute comparatively less.","title":"Biplot"},{"category":"section","location":"pca_tutorial/#Inverse-transformation","page":"Principal Component Analysis","text":"We just saw thatpca_transform projects the original data matrix down to component scores. The other function pca_invtransform maps the scores back into the original four measurements. If we keep all the components, we should be able to recover the data matrix exactly. \n\nX_recon = pca_invtransform(m, scores)\nmaximum(abs.(X .- X_recon))   \n\nWe see from the above output, keeping all the components, we are able to reconstruct X almost exactly. Now if we keep only the first two components, this gives us a low-rank approximation which we can see from the output below, it is close but not exact as the difference between the two matrices are larger than when we used all components for the reconstruction. \n\nm2 = pca(X; k = 2, standardize = true)\nX_approx = pca_invtransform(m2, pca_transform(m2, X))\nmaximum(abs.(X .- X_approx))      # small, but nonzero","title":"Inverse transformation"},{"category":"section","location":"pca_tutorial/#Summary","page":"Principal Component Analysis","text":"Here we demonstrated the use of PCA using the pca function from the Julia package BigRiverEssence.jl. PCA can also be applied to any similar dataset where the goal is dimension reduction.","title":"Summary"},{"category":"section","location":"#BigRiverEssence","page":"Home","text":"(Image: CI) (Image: codecov) (Image: Stable) (Image: Pkg Status)","title":"BigRiverEssence"},{"category":"section","location":"#Description","page":"Home","text":"This package provides efficient implementations of matrix decomposition and multivariate dimension reduction methods, including Principal Component Analysis (PCA)[1], Sparse Principal Component Analysis (SPCA)[2], Penalized Matrix Decomposition (PMD)[2], Canonical Correlation Analysis (CCA)[3], Sparse Canonical Correlation Analysis (SCCA)[4], Joint and Individual Variation Explained (JIVE)[5], Sparse Partial Least Squares Discriminant Analysis (SPLSDA)[6][7], and Partial Least Squares Kernel Regression (PLSkern)[8].\n\nThese methods are useful for extracting low-dimensional structure from high-dimensional data, identifying sparse latent components, and analyzing shared and individual variation across multiple datasets. The package is intended for high-dimensional applications such as high-throughput biological data.\n\nThe core routines rely on matrix operations for fast computation and are designed to support exploratory data analysis, feature extraction, and integrative analysis of complex datasets.","title":"Description"},{"category":"section","location":"#Installation","page":"Home","text":"The BigRiverEssence package can be installed by running: \n\nusing Pkg\nPkg.add(\"BigRiverEssence\")\n\nor from the julia REPL, press ] to enter pkg mode, and execute the following command:\n\nadd BigRiverEssence\n\nFor the most recent (development) version, use:\n\nusing Pkg\nPkg.add(url = \"https://github.com/senresearch/BigRiverEssence.jl\", rev=\"main\")","title":"Installation"},{"category":"section","location":"#Contributing","page":"Home","text":"We appreciate contributions from users including reporting bugs, fixing issues, improving performance and adding new features.","title":"Contributing"},{"category":"section","location":"#Questions","page":"Home","text":"If you have questions about contributing or using BigRiverEssence package, please communicate with the authors via GitHub.","title":"Questions"},{"category":"section","location":"#References","page":"Home","text":"[1]: Pearson, K. (1901). On Lines and Planes of Closest Fit to Systems of Points in Space. Philosophical Magazine, 2(11), 559–572.\n\n[2]: Witten, D. M., Tibshirani, R., & Hastie, T. (2009). A Penalized Matrix Decomposition, with Applications to Sparse Principal Components and Canonical Correlation Analysis. Biostatistics, 10(3), 515–534.\n\n[3]: Weenink, D. (2003). Canonical Correlation Analysis. Institute of Phonetic Sciences, University of Amsterdam, Proceedings 25, 81–99.\n\n[4]: Witten, D. M., & Tibshirani, R. (2009). Extensions of Sparse Canonical Correlation Analysis with Applications to Genomic Data. Statistical Applications in Genetics and Molecular Biology, 8(1), Article 28.\n\n[5]: Lock, E. F., Hoadley, K. A., Marron, J. S., & Nobel, A. B. (2013). Joint and Individual Variation Explained (JIVE) for Integrated Analysis of Multiple Data Types. Annals of Applied Statistics, 7(1), 523–542.\n\n[6]: Lê Cao, K.-A., Boitard, S., & Besse, P. (2011). Sparse PLS Discriminant Analysis: Biologically Relevant Feature Selection and Graphical Displays for Multiclass Problems. BMC Bioinformatics, 12, 253. \n\n[7]: Lê Cao, K.-A., Rossouw, D., Robert-Granié, C., & Besse, P. (2008). A Sparse PLS for Variable Selection when Integrating Omics Data. Statistical Applications in Genetics and Molecular Biology, 7(1), Article 35. \n\n[8]: Dayal, B. S., & MacGregor, J. F. (1997). Improved PLS Algorithms. Journal of Chemometrics, 11(1), 73–85.","title":"References"}]
}
